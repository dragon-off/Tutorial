<grid-form version="1.0" writer="com.qmatic.web.serversettings.PolicyWriterApi">
    #cdata("label" $text.getText("apiclient.loginPolicy"))

        #set($policy  = $dao.getPolicy("LOGON", "QWIN_APICLIENT"));
        #set($item  = $policy.getPolicyGroup("ENTER_USER_NAME"));
        <field type   = "checkbox" helpid = "apiclient.${item.getName()}">
            #cdata("name" $item.getName())
            #cdata("label" $text.getText("apiclient.${item.getName()}"))
            #cdata("init-value" "$item.isEnabled()")
        </field>

        #set($item  = $policy.getPolicyGroup("LIST_USER_NAME"));
        <field type   = "checkbox" helpid = "apiclient.${item.getName()}">
            #cdata("name" $item.getName())
            #cdata("label" $text.getText("apiclient.${item.getName()}"))
            #cdata("init-value" "$item.isEnabled()")
        </field>

        #set($item  = $policy.getPolicyGroup("WINDOWS_USER_NAME"));
        <field type   = "checkbox" helpid = "apiclient.${item.getName()}">
            #cdata("name" $item.getName())
            #cdata("label" $text.getText("apiclient.${item.getName()}"))
            #cdata("init-value" "$item.isEnabled()")
        </field>


        #separator("apiclient.passwordSett")
        #set($item  = $policy.getPolicyGroup("PSW_NEEDED"));
        <field type   = "checkbox" helpid = "apiclient.${item.getName()}">
            #cdata("name" $item.getName())
            #cdata("label" $text.getText("apiclient.${item.getName()}"))
            #cdata("init-value" "$item.isEnabled()")
        </field>

</grid-form>