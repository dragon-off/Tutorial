#loadcache2("INTERFACE*" true)

<grid-form version="1.0"
    writer="com.qmatic.web.serversettings.DefaultWriter">
    #set($name=$text.getText("intf.output"))
    #cdata("label" "$name $item")

    #set( $section                = "INTERFACE_SETTING_$group"                  )

    ##    SECTION   KEY            LABEL_ID             TYPE       HELP_ID           MIN MAX REGEXP
    ##---------------------------------------------------------------------------------------------

    #qsys($section  "OUTPUT$item"  "intf.outputProg"    "script"   "intf.outputProg"  0  0   "" )

</grid-form>
